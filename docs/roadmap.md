# What To Do Next, Pros...

Don't touch views.

Don't touch templates.

Don't touch URLs.

Don't touch dashboard.

Next session should be:

```text
1. Create models.py for every app
2. Define ForeignKeys
3. Define choices
4. Add __str__()
5. Register models in admin.py
6. Run makemigrations
7. Run migrate
```